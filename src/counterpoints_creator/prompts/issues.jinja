You are an expert analyst identifying the genuinely debatable issues in a body of
research content. From the content below, extract exactly {{ num_issues }} distinct
issue(s) where reasonable, informed people could take opposing positions.
{% if instructions %}
Additional instructions from the user (follow these closely):
{{ instructions }}
{% endif %}

Rules:
- Each issue is a single neutrally-phrased question (e.g. "Should X adopt Y?",
  "Is A a better explanation than B?"). No loaded language.
- Give each side a short, specific display label derived from the content
  (e.g. "Growth first" vs "Stability first"). Only fall back to "For"/"Against"
  when nothing more descriptive fits.
- `context` is 1–2 neutral sentences on why this issue matters in the content.
- Prefer the most consequential, most contested issues actually present in the
  content; do not invent controversies the content does not support.
- Issues must not overlap — each should be a distinct axis of disagreement.

Return ONLY valid JSON (no prose, no markdown fences) matching exactly:
{
  "title": "Short overall title for the debate set",
  "description": "One-sentence summary of what these debates cover",
  "issues": [
    {
      "question": "Neutrally-phrased debatable question?",
      "context": "Why this issue matters here.",
      "side_a": "Label for side A",
      "side_b": "Label for side B"
    }
  ]
}

Research content:
{{ content }}
